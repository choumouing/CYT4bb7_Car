# CYT4BB7 小车中文注释计划

## 概要

按“底层到顶层”分批注释：先公共工具和硬件驱动，再协议通信，再估计融合，最后控制调度和菜单。只注释 `project/code/` 业务代码，厂商库 `libraries/` 不动，除非发现总头文件暴露关系有问题。

注释风格统一中文、简洁口语，讲清楚“这个接口给谁用、什么时候调、数据单位是什么、异常/超时怎么处理”。`reset` 类函数默认不展开注释，除非它影响安全状态或模式切换。

## 注释顺序

1. `Common/`
   - 注释 `car_math`、`car_filter` 的公共接口用途。
   - 顺手检查限幅、死区、角度归一化等通用函数有没有在别处重复造轮子，艹，重复写限幅这种活最该先收拾。

2. `HW_Drivers/`
   - 顺序：`Motor`、`Encoder`、`IMU`、`UWB`、`CameraSpi`。
   - 头文件重点写引脚、PWM/串口/SPI通道、初始化和采样/输出接口。
   - `.c` 文件只给关键代码块加注释，例如电机方向处理、编码器读取、IMU更新、UWB帧解析、SPI传输状态。

3. `Protocols/`
   - 顺序：`SBUS`、`WirelessControl`、`CameraSpi`、`AirComm`、`Wifi`。
   - 协议头文件必须讲清楚帧格式、字段含义、超时策略、ACK/错误状态。
   - 车空串口通信 `AirComm` 单独重点注释：帧头、长度、类型、payload、CRC、ACK重试、心跳在线判断。
   - 三路摄像头 SPI 协议讲清楚主从关系、上下行 payload、ready/diag/target 数据含义。

4. `Estimation/`
   - 顺序：`Attitude`、`Position`、`Beacon_Detection`。
   - 重点注释 IMU 姿态更新、编码器里程计、UWB/加速度修正、信标目标估计。
   - 对 `odometer.c` 这类宏很多的文件，先给参数分组注释，再检查是否有通用数学宏能收敛到 `car_math`。

5. `Controller/` 和 `Menu/`
   - 顺序：`pid`、`control`、`Planner`、`car_mode*`、`car_start_sbus`、`car_loop`，最后 `Menu`。
   - `car_loop` 注释各频率任务职责：`1000HZ`、`100HZ`、`50HZ`、`25HZ` 分别干什么，哪些不能阻塞。
   - 控制层重点写输入来源、目标速度/角速度单位、安全停机逻辑、模式切换条件。

## 清理检查规则

- 不做大重构，只在注释时记录或小范围处理明显问题。
- 发现重复限幅、绝对值、死区、线性映射，优先复用 `car_math`。
- 发现只包一层、没实际意义、两三行且没有抽象价值的函数，标记为可合并候选。
- 发现无调用宏、无调用函数，先用 `rg` 查全工程调用点，再决定是否列入清理清单。
- 公开头文件继续遵守：业务头文件只 include `zf_common_headfile.h`，新增/调整接口必须通过总头文件统一暴露。

## 每批验收

- 头文件：每个对外接口都有中文说明，尤其是 `init/update/poll/get/set`。
- 源文件：关键代码块有注释，不逐行废话。
- 协议模块：正常帧、错帧头、长度异常、校验失败、超时恢复的处理路径能从注释看懂。
- 控制/估计模块：单位、频率、安全状态写清楚。
- 每批完成后给一份简短报告：已注释文件、发现的冗余宏/函数、建议后续清理项、需要硬件验证的点。

## 假设

- 第一轮只处理 `project/code/`，不注释 SeekFree/SDK 厂商库。
- 不主动改 git 分支、commit、push。
- 不注释普通 `reset` 函数，除非它关系到急停、在线状态、模式切换或控制输出归零。
- 注释以“看代码时快速理解”为目标，不写论文，也不把每句代码都翻译一遍。

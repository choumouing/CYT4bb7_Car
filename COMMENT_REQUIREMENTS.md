# CYT4BB7 小车项目中文注释要求

本文档是本项目后续所有代码注释工作的统一要求。后续给任何库、模块、函数补中文注释时，都按这里执行。别东一榔头西一棒槌，注释风格乱了，后面看代码比调车还难受。

## 1. 总体目标

- 注释只服务一个目的：让后来的人快速看懂关键函数、关键数据流、关键协议和关键硬件配置。
- 注释全程使用中文，表达要简洁、口语、直接，不能写得晦涩像论文。
- 注释要讲“为什么这么做”和“这一块整体干什么”，不要逐句翻译代码。
- 注释时顺手检查代码是否有明显冗余、重复、可合并或不规范的地方，但不要借注释名义乱做大重构。
- 注释顺序按从底层到顶层推进，先底层库，再协议和估计，最后控制调度和菜单。

## 2. 注释范围与层级顺序

推荐按以下顺序分批注释：

1. `project/code/Common/`
   - 公共数学、滤波、限幅、死区、线性映射等通用工具。
2. `project/code/HW_Drivers/`
   - 电机、编码器、IMU、UWB、Camera SPI 等硬件驱动。
3. `project/code/Protocols/`
   - SBUS、无线控制、WiFi、车空串口通信、三路摄像头 SPI 汇聚协议。
4. `project/code/Estimation/`
   - 姿态、位置、里程计、UWB 修正、信标目标估计。
5. `project/code/Controller/`
   - PID、底盘控制、模式切换、任务状态机、主循环调度、规划器。
6. `project/code/Menu/`
   - 参数显示、菜单配置、Flash 参数存取入口。

厂商库和 SDK 默认不注释、不修改。除非确认某个厂商接口是业务理解的关键入口，否则别碰。

## 3. 头文件注释要求

头文件主要讲清楚对外接口，重点是别人怎么用这个模块。

必须说明：

- 模块职责：这个模块负责什么，不负责什么。
- 主要公开接口：`init`、`update`、`poll`、`get`、`set` 等函数分别干什么。
- 调用频率：例如 `*_update_1000HZ()`、`*_update_100HZ()`、`*_update_50HZ()`、`*_update_25HZ()`。
- 输入输出单位：例如 m、m/s、rad/s、degree、PWM 占空比、编码器计数。
- 状态结构体字段含义：尤其是 `online`、`valid`、`last_error`、`age_ms`、`sequence` 这类字段。
- 硬件引脚配置：电机方向引脚、PWM 通道、UART 端口、SPI 片选、INT/READY 引脚等。
- 通信协议设计：帧头、长度、类型、payload、校验、ACK、超时、重试、在线状态判断。

不需要重点注释：

- 普通 `reset` 函数，除非它会影响急停、安全状态、控制输出归零、在线状态或模式切换。
- 一眼就能看懂的简单 getter/setter。
- 只是重复函数名意思的废话注释。

业务头文件继续遵守项目硬规则：只包含 `zf_common_headfile.h`，不要私自 include 其他业务头文件。

## 4. 源文件注释要求

`.c` 文件注释重点放在关键代码块，不要每句话都注释。

推荐方式：

- 5 到 6 行左右为了完成同一件事情的代码块，前面加一句中文注释。
- 状态机分支前说明该分支处理的状态和目的。
- 协议解析步骤前说明当前阶段，例如找帧头、读长度、收 payload、校验、分发。
- 控制算法前说明输入来源、输出目标和安全限制。
- 融合算法前说明信任哪个传感器、修正什么漂移、异常数据怎么丢弃。

避免方式：

- 不要把每一行代码翻译成中文。
- 不要写“给变量赋值”“调用函数”这种废话。
- 不要用注释掩盖烂代码。代码已经乱成一坨时，先指出问题，再做小范围整理。

## 5. 协议模块注释要求

协议相关模块必须比普通模块注释更详细，尤其是车空串口通信和三路摄像头 SPI。

车空串口通信必须讲清楚：

- 小车 CYT4BB7 与无人机 CYT4BB7 的角色关系。
- 串口号、波特率和收发入口。
- 帧格式：帧头、长度、消息类型、payload、CRC/校验。
- 消息类型：参数设置、函数执行、ACK、心跳、运行数据等。
- ACK 设计：哪些消息需要 ACK，超时时间，最大重试次数，失败后状态如何记录。
- 在线判断：心跳收发、离线超时、统计计数。
- 异常处理：半包、错帧头、长度异常、校验失败、队列溢出。

三路摄像头 SPI 必须讲清楚：

- 小车 CYT4BB7 是 SPI 主机，三块 CYT2BL3 是从机。
- 每个从机对应的摄像头编号和 ready/interrupt 信号。
- 下行 payload 和上行 payload 的字段含义。
- `sequence`、`ack_sequence`、`flags`、`peer_last_error` 的作用。
- 汇聚后的信标目标结构体如何表达相机编号、坐标、面积、更新时间和有效性。

## 6. 硬件驱动注释要求

硬件驱动注释要让人能快速知道线怎么接、数据怎么来、输出怎么打。

必须说明：

- 引脚、通道、方向反转、PWM 最大值等硬件配置。
- 初始化函数配置了哪些外设。
- 周期更新函数读取了什么硬件数据。
- 输出函数如何限幅、如何处理正反转、如何保证默认安全。
- 原始数据和工程单位之间的换算关系。

硬件驱动层只注释硬件抽象和原始数据获取，不要在注释里把规划、控制、融合职责往驱动层塞。

## 7. 控制与估计注释要求

控制和估计模块必须把频率、单位和安全逻辑写清楚。

估计模块重点说明：

- IMU 姿态更新使用哪些输入。
- 编码器里程计如何得到前进、横移、yaw 相关数据。
- UWB 如何提供长期不漂的位置参考。
- 信标检测结果如何被整理成控制可用的目标。
- 数据失效、超时、跳变、异常值如何处理。

控制模块重点说明：

- 速度目标来自哪里。
- PID 输入、输出和限幅单位是什么。
- yaw rate、yaw hold、速度环分别在哪个频率运行。
- 遥控失联、控制未使能、急停时如何停止电机。
- 模式切换时哪些运行状态需要清空。

## 8. 冗余检查要求

注释过程中要顺手检查以下问题，发现后先用 `rg` 查调用点，再决定是否整理或记录。

重点检查：

- 有没有重复写限幅、绝对值、死区、线性映射等通用函数。
- 通用数学函数优先放到 `project/code/Common/car_math.*`。
- 通用滤波逻辑优先放到 `project/code/Common/car_filter.*`。
- 有没有无用宏定义、无调用函数、只包一层没意义的函数。
- 有没有一个函数只调用另一个函数，且没有额外语义或隔离价值。
- 有没有两三行函数纯粹增加跳转成本，保留它又不能提升可读性。
- 有没有模块跨层访问私有状态、偷摸依赖全局变量。

整理原则：

- 小范围、低风险、明显正确的冗余可以随注释批次处理。
- 涉及接口变化、跨模块调用、控制安全的整理必须单独列出来，不要顺手乱改。
- 不为了“看起来高级”新增抽象。KISS、YAGNI、DRY 必须优先。

## 9. 每批注释完成后的交付要求

每完成一个库或一批模块，需要给出简短说明：

- 本批注释了哪些文件。
- 头文件对外接口是否已经讲清楚。
- 关键 `update`、`poll`、协议解析、控制代码块是否已经补注释。
- 发现了哪些无用宏、重复函数、可合并函数或可下沉到 `Common` 的工具函数。
- 哪些修改需要 IAR 构建验证。
- 哪些控制、电机、通信改动需要硬件验证。

如果只是注释，不改行为，也要说明“本批只改注释，不改变运行逻辑”。

## 10. 禁止事项

- 禁止把注释写成逐行翻译。
- 禁止写晦涩、装腔、论文味很重的注释。
- 禁止注释和代码行为不一致。
- 禁止在业务头文件里私自 include 其他业务头文件。
- 禁止借注释机会大规模重构。
- 禁止为了未来可能用到的东西提前设计一堆接口。
- 禁止没有确认就删除文件、批量替换、改 git 历史、提交、推送。

## 11. 推荐注释模板

头文件模块说明：

```c
/*
 * 模块作用：
 * 这里负责 xxx，对外提供 xxx 数据/控制入口。
 * 调用节奏：
 * init 在系统启动时调用，update_xxxHZ 在主循环对应频率调用。
 * 注意：
 * 单位是 xxx，超时后 valid/online 会变成 0。
 */
```

函数说明：

```c
/*
 * 更新 xxx 状态。
 * 输入来自 xxx，输出写入 xxx。
 * 这个函数每 xxxHz 调一次，里面不能阻塞。
 */
```

代码块说明：

```c
/* 先把原始输入限幅，避免后面的 PID 被异常值干翻。 */
```

协议说明：

```c
/*
 * 帧格式：
 * header + length + type + payload + crc
 * length 只表示 payload 长度，crc 覆盖 type 和 payload。
 * 收到异常帧直接丢弃，并累计错误计数。
 */
```
